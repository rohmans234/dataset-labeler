services:
  # Layanan Web App (Next.js)
  labelflow-web:
    build:
      context: ./dataset-labeler
      dockerfile: Dockerfile
    container_name: labelflow_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # Contoh penambahan env untuk Google Drive jika diperlukan nanti
      # - GOOGLE_SHEETS_ID=${GOOGLE_SHEETS_ID}
    restart: always

  # Layanan n8n
  n8n:
    image: n8nio/n8n:latest
    container_name: n8n_workflow
    ports:
      - "5678:5678"
    volumes:
      - ./n8n-data:/home/node/.n8n
    environment:
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - NODE_ENV=production
      - WEBHOOK_URL=http://localhost:5678/
    restart: always